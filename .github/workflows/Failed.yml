on:
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - name: callSuccessOrFailed.yml
        uses: gsino/GitHubActionsTest/.github/workflows/SuccessOrFaild.yml@master
        with:
          isSuccess: false
